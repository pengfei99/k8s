apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: mario
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  tls:
    - hosts:
        - pengfei.kub.sspcloud.fr
  rules:
    - host: pengfei.kub.sspcloud.fr
      http:
        paths:
          - path: /
            backend:
              serviceName: mario
              servicePort: http
